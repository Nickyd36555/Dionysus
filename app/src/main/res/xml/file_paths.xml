<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Where downloaded update APKs live before install. -->
    <external-cache-path name="updates" path="updates/" />
    <cache-path name="cache_updates" path="updates/" />
    <!-- App-scoped downloads (offline playback). -->
    <external-files-path name="downloads" path="downloads/" />
</paths>
